# دليل نظام فاتورتك

*آخر تحديث: 2025 | Laravel 10 / PHP 8.2*

---

## أولاً: البدء السريع للعميل الجديد

```
1.  سجّل حسابك          /register
2.  أعدّ بيانات الشركة   /settings
3.  أضف مخازنك           /warehouses
4.  أضف الفئات والوحدات  /categories
5.  أضف منتجاتك          /products/create
6.  أضف عملاءك           /customers/create
7.  أضف موردينك          /suppliers/create
8.  أضف موظفيك           /users/create
9.  ابدأ الفوترة          /invoices/create
10. سجّل مشترياتك        /purchases/create
11. تابع التقارير         /reports
12. أجرِ جرداً دورياً     /stocktaking
```

---

## ثانياً: الوحدات والمسارات

### لوحة التحكم `/dashboard`
- إيرادات الشهر، صافي الربح، عدد العملاء
- منتجات منخفضة المخزون، آخر 5 فواتير
- رسم بياني لإيرادات آخر 6 أشهر
- **صافي الربح = المحصّل - تكلفة البضاعة - المصروفات**

---

### الفواتير `/invoices`

**حالات الفاتورة:**
```
draft → sent → partially_paid → paid
                              → overdue → cancelled / returned
```
- المخزون يُخصم عند الانتقال إلى `sent` أو `paid`
- مشاركة بـ PDF، واتساب، بريد، أو رابط عام `/invoice/{uuid}/view`

**حساب الإجمالي:**
```
المجموع = Σ (الكمية × السعر)
الضريبة = تُحسب بعد خصومات الأسطر
الإجمالي = المجموع + الضريبة - خصم الرأس
```

---

### المدفوعات
- دفعات جزئية أو كاملة بطرق متعددة
- تحديث تلقائي لحالة الفاتورة عند اكتمال الدفع

---

### مرتجعات المبيعات `/invoices/{id}/return`
- شرط: الفاتورة بحالة `sent / paid / partially_paid`
- يُعيد المخزون + يُخفض إجمالي الفاتورة + يُحدّث الحالة تلقائياً

---

### العملاء `/customers`
- قاعدة بيانات + سجل كامل للفواتير + إجمالي المبيعات والمحصّل والمتبقي

---

### المشتريات `/purchases`

**حالات أمر الشراء:**
```
pending → received  (يُضاف للمخزون + يُحدَّث WAC)
        → cancelled
```
**WAC = (الكمية القديمة × التكلفة القديمة + الكمية الجديدة × التكلفة الجديدة) ÷ الإجمالي**

---

### مرتجعات المشتريات `/purchases/{id}/return`
- شرط: الأمر بحالة `received`
- يُخصم المخزون من المخزن المحدد + يُخفض إجمالي الأمر

---

### المنتجات `/products`
- SKU، باركود، فئة، وحدة قياس، سعر بيع، سعر تكلفة، ضريبة
- كمية لكل مخزن في جدول `warehouse_stocks`
- تعديل يدوي: إضافة (in) / خصم (out) / تسوية (adjustment)

---

### المخازن `/warehouses`
- مخازن متعددة، مخزن افتراضي واحد
- نقل مخزون بين مخزنين
- تصفير / تفعيل / تعطيل مخزن

---

### الجرد الدوري `/stocktaking`
```
إنشاء جلسة (مخزن واحد) → إدخال الكميات الفعلية → مراجعة الفروقات → اعتماد (لا رجعة)
```

---

### نقطة البيع POS `/pos`
- واجهة كاشير، إرسال AJAX، يحسب الباقي من الدفع
- إنشاء عميل جديد من الواجهة مباشرة

---

### البيع السريع `/quick-sale`
- يدعم البيع بالآجل (credit)
- يدعم دفع جزئي مع تحديد المبلغ
- بحث منتج بالباركود أو SKU
- عميل نقدي افتراضي يُحفظ في الإعدادات

---

### المصروفات `/expenses`
- فئات مخصصة، دعم عملات متعددة مع سعر صرف
- تُدرج تلقائياً في تقرير الأرباح والخسائر

---

### التقارير `/reports`

| التقرير | المحتوى |
|---------|---------|
| المبيعات | إجمالي، محصّل، متبقي، ضريبة، توزيع حسب موظف/طريقة دفع |
| المشتريات | إجمالي، مستلم، معلق، فلترة بمورد وحالة |
| المخزون | كميات لكل مخزن، قيمة إجمالية |
| العملاء | ترتيب بالمبيعات، نسبة التحصيل |
| الأرباح والخسائر | إيرادات - COGS = ربح إجمالي - مصروفات = صافي الربح |

جميع التقارير تدعم التصدير بـ Excel.

---

### المستخدمون والصلاحيات `/users`

**الأدوار الجاهزة:**
| الدور | الصلاحيات |
|-------|-----------|
| مدير كامل | كل شيء |
| مبيعات | فواتير + عملاء + منتجات (عرض) |
| مشتريات | مشتريات + موردون + منتجات |
| محاسب | فواتير + مصروفات + تقارير |
| مخزن | منتجات + مخازن + جرد |
| مخصص | صلاحيات يدوية |

- **Admin** يرى كل فواتير الشركة
- **غير Admin** يرى فواتيره الخاصة فقط
- كل مستخدم يُربط بمخزن أو أكثر

---

### الإعدادات `/settings`
- شعار، عملة افتراضية، نسبة ضريبة، بادئة فاتورة
- توقيع إلكتروني، ختم، شروط وأحكام افتراضية
- طرق الدفع المتاحة

---

## ثالثاً: تدفق المخزون

```
رصيد افتتاحي
    ↓ + استلام مشتريات (received)
    ↓ - فاتورة مرسلة (sent/paid)
    ↓ + مرتجع مبيعات
    ↓ - مرتجع مشتريات
    ↓ ± جرد دوري (تسوية)
```

---

## رابعاً: أسئلة شائعة

**هل يمكن فاتورة بدون منتج من المخزون؟**
نعم، بإضافة أسطر يدوية بوصف نصي.

**ماذا يحدث عند حذف فاتورة؟**
المخزون لا يُعاد تلقائياً — سجّل مرتجعاً أو عدّل يدوياً.

**هل يمكن مخزن أمر شراء في مخزن مختلف عن مخزن المستخدم؟**
نعم، يمكن اختيار أي مخزن متاح عند إنشاء الأمر.

**كيف تعمل العملة؟**
عملة واحدة محددة في الإعدادات لكل الفواتير والمشتريات.

**الفرق بين البيع السريع ونقطة البيع؟**
- البيع السريع: يدعم الآجل والدفع الجزئي، واجهة form عادية
- نقطة البيع: كاشير AJAX، يحسب الباقي، لا يدعم الآجل
